conda install -c conda-forge pystac-client stackstac xarray dask matplotlib sentinelsat
pip install rasterio, tqdm, planetary-computer, odc-stac, scipy

# Core dependencies
pystac-client
stackstac
xarray
dask
matplotlib
rasterio
planetary-computer
odc-stac
numpy

# Testing
pytest
pytest-cov

# Optional: distributed processing
dask[distributed]

# Optional: progress bars
tqdm